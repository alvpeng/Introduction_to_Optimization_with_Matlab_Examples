% 例程 5.11  仓库问题续例（更快的动态规划解法）
Cw = 0;  %  最优（最大）存储容量
R = [];
%  货物出入库记录
L = [12, -3, 15, -6, 14, 30, -25, -3, 37, 4, -8, -14];
n = length(L);
R(1) = 0;
for j = 1: n
R(j + 1) = max([0, L(j) + R(j)]);
if (R(j) > Cw)
Cw = R(j);
end;
end;
%  输出结果（所需的最大存储容量）
Cw
