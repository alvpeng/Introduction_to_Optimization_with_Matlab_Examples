% 例程 5.9  仓库问题示例（暴力解法）
%  暴力解法
Cw = 0;  %  最优（最大）存储容量
%  货物出入库记录
L = [12, -3, 15, -6, 14, 30, -25, -3, 37, 4, -8, -14];
n = length(L);
for i = 1: n
for j = i + 1: n
su = max([0, sum(L(i: j))]);
if (su > Cw)
Cw = su;
end;
end;
end;
%  输出结果（所需的最大存储容量）
Cw
